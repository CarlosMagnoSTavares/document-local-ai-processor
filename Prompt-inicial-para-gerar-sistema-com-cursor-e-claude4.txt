Aja como um arquiteto de software, desenvolvedor fullstack e engenheiro de IA. Sua tarefa é criar um backend escalável, seguro e leve, que atue como um orquestrador para análise de documentos com auxílio de modelos de linguagem locais (via Ollama).
📥 Requisitos Funcionais
Endpoint principal: POST /upload
Recebe um arquivo no body (ex: arquivo.pdf, arquivo.jpg, arquivo.png, arquivo.docx, arquivo.xlsx, etc.).
Recebe parâmetros obrigatórios no header:
Header	Descrição
Prompt	Exemplo: "Verifique qual CNPJ existe nesse documento" (Obrigatório)
Format-Response	Exemplo: "[{\"CNPJ\": \"\"}]" (Obrigatório)
Example	Exemplo: "[{\"CNPJ\": \"XX.XXX.XXX/0001-XX\"}]" (Opcional)
Model	Exemplo: "gemma3:1b" (Obrigatório)
Key	Chave de autenticação para uso da API. Deve ser validada com valor vindo do .env (Obrigatório)
🛠️ Requisitos Técnicos
Instalar e rodar via Docker com base em imagem Ubuntu LTS.
Usar OCR (Tesseract) para imagens.
Usar PDF-to-Text, DOC/DOCX/Excel parser para documentos.
Enfileirar o processamento:
Fila para extração de texto (OCR ou parsing).
Fila para envio do prompt à LLM local via Ollama.
Fila para formatação e armazenamento da resposta.
Utilizar Ollama com modelo gemma3:1b (outros modelos podem ser usados futuramente).
Criar um sistema de filas para permitir processamento assíncrono e escalável.
O backend deve:
Rodar em máquinas leves com modelos pequenos (ex: gemma3:1b).
Escalar em máquinas potentes, utilizando paralelização inteligente baseada no hardware.
Banco de dados:
Usar MySQL ou SQLite (foco em leveza).
Persistir perguntas e respostas por até 1 hora (máximo 24h).
Implementar limpeza automática para evitar acúmulo de arquivos e logs.
📊 Endpoints adicionais
GET /queue
Retorna a lista de documentos na fila e seus status:
Upload realizado
Texto extraído
Prompt processado
Erro (se houver)
GET /response/:id
Retorna a resposta da LLM para o documento específico.
📁 Organização do Projeto
O projeto deverá incluir:
Dockerfile completo com todas dependências instaladas (Tesseract, Python libs, OCR tools, etc.).
Instalação e configuração do Ollama com o modelo gemma3:1b.
Código backend completo, pronto para rodar.
Endpoints REST documentados.
Scripts de inicialização.
Sistema de filas.
Validação de headers obrigatórios.
Logs organizados e rotacionados.
🧪 Testes
Inclua exemplos de chamadas curl para teste.
Utilize o arquivo @teste.jpg presente na pasta como exemplo.
Forneça também uma collection do Postman com todos os endpoints prontos para uso.
📄 Documentação
Criar um README completo, contendo:
Instalação passo a passo (Docker, dependências, env).
Como rodar a aplicação.
Como testar.
Como escalar a aplicação.
Como modificar o modelo Ollama.
Explicação sobre cada endpoint e seus headers.
Estratégia de limpeza automática dos dados.
🧠 Considerações sobre Escalabilidade
O sistema deve ser capaz de:
Rodar em máquinas modestas sem sobrecarga.
Escalar automaticamente (threads, workers, filas).
Paralelizar tarefas (extração, prompt, formatação).
Não depender de serviços externos além da LLM local e banco leve.
🛡️ Segurança
As requisições só serão aceitas com Key válida (armazenada no .env).
A aplicação deve tratar falhas de upload, parsing, e resposta com mensagens claras.
Evitar upload de arquivos potencialmente maliciosos (limitar extensões permitidas).
Limitar tamanho dos arquivos para evitar sobrecarga.
💻 Ambiente
Ambiente atual:
Windows 11
Docker instalado
docker -v
Docker version 27.5.1, build 9f9e405
Conectado à internet para baixar pacotes e imagens necessárias.